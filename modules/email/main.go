package main

// Email module main entry point
// This file can be used when running email as a standalone service
// For now, it's integrated into the monolith via the router
func main() {
	// Placeholder for standalone service mode
	// Currently, the Email module is registered in cmd/server/main.go
}
